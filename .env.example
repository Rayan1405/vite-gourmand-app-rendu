# Server
PORT=3000

# Base relationnelle (MySQL - optionnel)
# Si DB_NAME/DB_HOST/DB_USER sont renseignes, l'application utilise MySQL.
# Sinon, elle bascule automatiquement sur SQLite (data/app.db).
DB_HOST=127.0.0.1
DB_PORT=8889
DB_NAME=Vite_Gourmand
DB_USER=root
DB_PASSWORD=your_mysql_password

# MongoDB Atlas (NoSQL)
MONGODB_URI=mongodb+srv://your_mongo_user:your_mongo_password@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0
MONGODB_DB=vite_gourmand
MONGODB_COLLECTION=order_analytics

# SMTP (required for real emails)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_FORCE_IPV4=true
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
MAIL_FROM="Vite and Gourmand <your-email@gmail.com>"
MAIL_TO=your-email@gmail.com
